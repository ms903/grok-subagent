interface:
  display_name: "Grok Subagent"
  short_description: "Delegate coding and Grok-native X/web research to Grok agents"
  default_prompt: "Use $grok-subagent to ask Grok for an independent read-only investigation or real-time X/web search."
dependencies:
  tools:
    - type: "mcp"
      value: "grok-subagent"
      description: "Local bridge to authenticated Grok Build ACP agents and isolated Grok search"
policy:
  allow_implicit_invocation: true
