interface:
  display_name: "Grok Subagent"
  short_description: "Run Grok through responsive Luna or Terra monitors"
  default_prompt: "Use $grok-subagent with a native GPT-5.6 Luna monitor to run Grok, relay compact public progress, preserve Plan approvals for me, and verify the result."
dependencies:
  tools:
    - type: "mcp"
      value: "grok-subagent"
      description: "Local bridge for authenticated Grok Build ACP control and isolated Grok search"
policy:
  allow_implicit_invocation: true
